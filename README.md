# Приложение для взаимодействия с БД

## Содержание
- [🏗️ Стек технологий](#-стек-технологий)
- [🚀 Подготовка к запуску](#-подготовка-к-запуску)
- [⚡ Запуск](#-запуск)
- [⚙️ Переменные окружения](#-переменные-окружения)
- [🔌 Ссылки](#-ссылки)
- [🗄️ Администрирование БД](#-администрирование-базы-данных)
## Стек технологий
#### Backend
- **Java 17** - Основной язык программирования
- **Spring Boot 3.5.6** - Фреймворк для создания приложений
- **Spring Data JPA** - Работа с базой данных
- **Hibernate** - ORM framework
- **Maven** - Управление зависимостями

#### База данных
- **PostgreSQL 16** - Основная реляционная база данных
- **HikariCP** - Пул соединений
- **Liquibase** - Миграции базы данных

#### Frontend
- **React 18** - UI библиотека
- **TypeScript** - Типизированный JavaScript
- **Vite** - Сборщик и dev-сервер
- **Antd** - Стилевой фреймворк
- **Axios** - HTTP клиент

#### Контейнеризация & Инфраструктура
- **Docker** - Контейнеризация приложений
- **Docker Compose** - Оркестрация контейнеров
- **PgAdmin 4** - Веб-интерфейс для управления БД


## Подготовка к запуску
Перед началом работы убедитесь, что у вас установлены:
* **Docker**: 20.10+
* **Docker Compose**: 2.0+
* **Git**

 #### Проверка установки:
```
docker --version
docker-compose --version
git --version
```


##  Запуск
* Склонируйте репозиторий:
```
git clone <repository-url>
cd <repository-directory>
```
* Введите команду в консоль:
```
# Запуск всех сервисов в фоновом режиме
docker-compose up -d

# Или с принудительной сборкой образов
docker-compose up -d --build

# Или с просмотром логов в реальном времени
docker-compose up
```
* Можете проверить статус командой:
```
docker-compose ps
```
* Остановка приложения:
```
# Остановка с сохранением данных
docker-compose down

# Остановка с удалением томов (данные БД будут удалены)
docker-compose down -v
```

##  Ссылки
* Frontend доступен по [этой](http:localhost:5173) ссылке
> Примечание: ссылка по умолчанию.

## Переменные окружения
### Backend
| Переменная  | 	Значение по умолчанию |Описание|
| ------------- |:-------------:|:--------------|
| `DB_URL`      | postgres-16:5432     |URL базы данных PostgreSQL|
| `DB_NAME`      | postgres     |	Имя базы данных|
| `DB_SCHEMA`      | public     |	Схема базы данных|
|`DB_USERNAME`|postgres|	Имя пользователя БД|
|`DB_PASSWORD`|root|	Пароль пользователя БД|
|`CONTAINER_NAME`|postgres-16|	Имя контейнера БД. Необходима для работы функций архивации и бэкапа. Менять только когда поменяли название контейнера PostgreSQL|
|`SERVER_PORT`|8080|	Порт Spring Boot приложения|

### Front
| Переменная  | 	Значение по умолчанию |Описание|
| ------------- |:-------------:|:--------------|
| `VITE_API_URL`      | http://localhost:8089/api     |URL API для работы с сущностями|
| `VITE_API_URL_SERVICE`      | http://localhost:8089/api     |	URL сервисного API|
| `VITE_API_URL_DATA`      | http://localhost:8089/api     |	URL API данных|


## Администрирование базы данных
### Доступ к PGAdmin
1.  Откройте http://localhost:8080
2.  Войдите с учетными данными:

- Email: `admin@example.com`

- Пароль: `admin`
## Настройка подключения в PGAdmin
1.  Правый клик на **"Servers"** → **Register** → **Server**

2.  Во вкладке **General**:

3.  Name: PostgreSQL Docker (или любое другое)

4.  Во вкладке **Connection**:

- Host: `postgres-16`

- Port: `5432`

- Username: `postgres`

- Password: `root`
